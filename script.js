const userInput = document.getElementsById("entry-input"); //text from input field
const addButton = document.getElementsById("add-entry-button"); //add entry to list
const clearButton = document.getElementById("clear-entries-button"); //clear entry list
const renderChoice = document.getElementById("render-choice"); //dropdown for render

function renderList(){
    if(renderChoice.value === 'option1'){
        //render option 1
        optionOne();
    }else{
        //render option 2
        optionTwo();
    };
};

function optionOne(){


};

function optionTwo(){


};

function storeData(){


};

function updateData(){


};